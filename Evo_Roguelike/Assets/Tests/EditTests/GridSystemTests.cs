using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using NUnit.Framework;
using UnityEngine.TestTools;
using static TreeEditor.TreeEditorHelper;
using UnityEditor.Rendering;
using UnityEngine.Rendering;
using UnityEngine.Tilemaps;

public class GridSystemTests
{
   /*private GridManager GetGridManagerObject()
    {
        NoiseGeneratorType noiseType = NoiseGeneratorType.Default;
        MaskGeneratorType maskType = MaskGeneratorType.Default;
        FaderType faderType = FaderType.Default;
        int terrainWidth = 720;
        int terrainHeight = 720;
        Tilemap groundTilemap = new Tilemap();
        List<GroundTile> groundTiles = new List<GroundTile> { };
        GridManager gridManager = new GridManager(noiseType, maskType, faderType, terrainHeight, terrainWidth, _groundTilemap,
                   _groundTiles, _noiseQuantizer, _bGenerateNewIslandOnGameStart);
    }

    // Test tilemap generation
    [Test]
    public void TestTilemapGeneration()
    {
        
       GenerateGroundTiles()

    }*/
    // Test ground tile info

    // Test ground data instances
}
